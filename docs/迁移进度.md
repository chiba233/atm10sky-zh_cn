# 从 atm10-zh-cn 迁移的进度

本文件只记**还没做完的部分**与做每一项时已经测得的数字，做完一项就删掉一项。
全部做完后删除本文件。

判据一律对着 ATM10 Sky 2.0.4 的真实字节算：模组是否存在按 jar 内 `assets/<命名空间>` 判，
不按 jar 文件名判（带连字符的文件名按名字前缀查会误判成缺失）。

整合包身份：ATM10SKY 2.0.4 / MC 1.21.1 / NeoForge 21.1.250 / manifest 326 条、mods 下 321 个 jar。
CurseForge 项目号 1298402（取自项目页的 Project ID 栏；ATM10 是 925200，两者不能混）。

## 已完成

- 构建管线整套（`scripts/` `.github/` `installer/` `src/rules/`）与全部根文档。
  包身份只钉在三处：`paths.py` 的 `PACK_NAME`、`paths.py` 的 `MODPACK_NAME`、
  `fetch_pack.py` 的 `PROJECT`。`MODPACK_NAME` 的消费者是 `generate_all.sh` 里那段
  manifest 校验——两个整合包的目录结构一模一样，`ATM_PACK_ROOT` 指错了会拿另一个包的
  字节生成一整套汉化且全程不报错。
- 任务书搬运脚本 `scripts/import_quest_lang.py` 取自同类迁移的那个仓库，自包含。

## 待办

### 一、`src/` 的译文与映射还没迁

要迁的是 `src/pack`、`src/books`、`src/vaultpatcher`、`src/kubejs`、
`src/optional-mods-pinyin`，以及 `src/` 根下那几个机器文件。
**不在搬家时替构建做取舍**——哪些模组这一版没有，由生成器对着本版字节现判。
迁完先 `git add` 再跑 `scripts/compliance/protect.py --update` 登记
（它靠 `git ls-files`，顺序反了会「新收 0 个」）。

### 二、`versions/2.0.4/` 与 `versions/db/2.0.4/` 的五份基线

jars / keybinds / quest_baseline / productive_trees / vaultpatcher，一律由 CI 生成、
下载 artifact 人工核对后入库，不把开发机算出来的数写进 `versions/db/`。

`default_resource_packs.txt` 取本地实例写全后的 `options.txt` 里 `resourcePacks:`
那一行逐项照抄。安装器靠它决定汉化包插在第几位，写错会让汉化包被压在内置包底下、
等于没启用。**游戏只跑到一半时读到的 `resourcePacks:[]` 是中间态，不能采信。**

### 三、任务书：搬完 4998 条，剩 322 条复核 + 1030 条从零翻

`scripts/import_quest_lang.py` 已在 CI 里跑过（流水线「任务书搬运（一次性）」，
`workflow_dispatch`）。中文那一侧不靠令牌也不取游戏目录：clone 上一个仓库（公开），
用**它自己的** `fetch_pack.py` 取它那一版的官方 overrides，再用**它自己的**
`gen_quest_lang_patches.py` 把它的 delta 并进上游 zh_cn——摊出来的就是它的出货树。
游戏目录那份是 `ftbquestslangsplitter` 运行时按哈希序合并的，值的形态与键的有无都会变。

实测结果（本包英文任务书 6350 个键）：

| | 条数 | 占比 |
|---|---|---|
| 原文逐字节一致 → 已搬 | 4998 | 78.7% |
| 原文改过 → 没搬，进复核清单 | 322 | 5.1% |
| 上一个仓库没有中文 → 从零翻 | 1030 | 16.2% |

落地形态：48 份 `zz_hanhua_<上游章节名>.snbt`，4998 键，**跨文件重复键 0**。
解析器自检 282 个输入文件「解析→回写」与原文逐字节相同。

判据是**英文原文逐字节一致**，不是任务 id。本包是空岛整合包，上游为此重写了不少任务
正文——实测相似度低于 0.7 的那批写着 `this is a Skyblock modpack! Instead…`、
`there is no underground here`、
`Stellarite spawns between Y Level- why am I even saying this? You know there isn't any-`。
按 id 套旧译文，玩家读到的是一段叫他下矿的中文，比留英文糟得多。

**文件名必须带 `zz_hanhua_` 前缀、与上游章节名脱钩**（`src/rules/quests.json` 有硬规则）。
「上游不带 zh_cn 所以用不上 delta 层」这个推论是错的：整合包升版会拆章节、改章节名，
按上游原名存的源文件到那时无法同时满足两个在册版本，而跨版本是这套结构的根本能力。

上一个仓库迁入的 15 份专属章节译文已移除（进 `released`，`approved` 记 `68209f7`）：
它们的 1877 键里 694 键已按本包章节重新落位、1110 键在本包英文任务书里不存在、
余下 73 键属于上面那 322 条复核清单。

待办：`build/quest_lang_review.txt` 里那 322 条逐条处理（`build/` 不入库，重跑流水线可再生）。
其中一类是**只差颜色码**（本包给章节标题加了 `&f`）或只差拼写修正
（`Possesion`→`Possession`、`usefull`→`useful`），中文照抄即可；另一类是正文真改写，
必须重译。

### 四、`src/upstream` 还剩 58 份映射的目标文件不在本版

77 份映射全部在库。按 `gen_upstream_patches.py` 自己的语义（`find` 是连续行块）对着
本版官方字节实测，现在的落点是：

| 情形 | 映射数 | 处理 |
|---|---|---|
| 目标文件在、改动全部套得上 | 19 份 / 落地 32 处 | 已按本版字节重推完 |
| 目标文件在本版不存在 | 58 份 / 214 处 | **待处理**，见下 |

重推做了两件事：

- 四份 kubejs 脚本（`tooltips.js`、`CustomAdditions.js`、`Universal_Press.js`、
  `RecipeViewer.js`）两边被上游整体重排过格式——源仓库那边是单引号带分号，本版是双引号
  不带分号，所以**没有一行能原样命中**。按字符串字面量逐条对照重写：英文内容相同的
  照搬原译文，一个字没改；本版新增的另译。`tooltips.js` 共换 33 条字面量
  （沿用 25 + 新译 8），`RecipeViewer.js` 在本版只剩移除配方的调用、没有可译文本。
- `title_screen_layout.txt` 重推完成。坐标不是问题：两个整合包的「选项」「退出」都在
  `y = 73`，汉化按钮的位置是跟着它们定的，所以照原样插两颗按钮、把那两颗挪到
  `y = 104` 即可。三处锚点都唯一，套用后 +114 行。
- 另有 8 份任务书章节的映射，目标文件在但 `hover` 字段一个不剩（上游把悬停文字外化成
  任务书 lang 的 `image.<ID>.title`），共 44 处改动已登记进
  `versions/2.0.4/unpatchable.json`，登记两头仍 fail-closed。

**剩下这 58 份的目标文件在本版整合包里整份不存在。** 逐份对着干净实例查过，
分三类，结论一致——本版没有这些落点：

| 类 | 份数 | 查到的事实 |
|---|---|---|
| 上游在本版没有这些脚本／章节 | 38 | `kubejs/server_scripts/mods/modular_machinery/**` 13 份（modularmachinery 的 assets/data 在 321 个 jar 里一个都没有）、`kubejs/client_scripts/ponder/**` 9 份（本版没有 ponder 目录）、`kubejs/startup_scripts/Modern-Industrialization/**` 6 份（模组在、但上游没放这些脚本）、`Theurgy` / `hyperbox` 各 1 份（两个模组都不在包里）、`announcements` / `Mekanism-Tooltips.js` / `incompatible_versions.js` / `update_checker.js` 4 份、以及 4 个本版没有的任务书章节（`bumblezone`、`chapter_2_the_star`、`iron_spells_and_spellbooks`、`relics`） |
| `config/mysticalcustomization/**` | 13 | `mysticalcustomization` 不在包里（包里只有 `mysticalagriculture` 与 `MysticalAgradditions`），整个 config 目录不存在 |
| `kubejs/assets/<命名空间>/lang/zh_cn.json` | 7 | **本版 ATM 不注入任何 zh_cn**：`kubejs/assets` 下 41 个命名空间只有 `pt_br.json`，`zh_cn.json` 一个都没有。那 7 份映射在上一个整合包是用来修 ATM 注入的中文的，本版没有可修的东西 |

`gen_upstream_patches.py` 对「目标文件整份不在」是硬退出，报错写的是「把映射一起删掉」；
`unpatchable.json` 登记的是「某一处改动在这一版套不上」，覆盖不到整份缺失。
删文件不由我执行——清单与逐类证据在上面，`protect.py` 还要求每条在 `released` 里留记录。

## 本版与上一个整合包的一处结构差别（影响译文怎么落）

上一个整合包里 ATM 用 `kubejs/assets/<命名空间>/lang/zh_cn.json` 注入中文，**它压过资源包**，
所以那边改物品名必须走 `src/upstream` 去改注入的那一份。

**本版没有这一层**（41 个命名空间只有 `pt_br.json`）。也就是说这些命名空间的中文由
「模组 jar 自带 zh_cn + 本包资源包」决定，`src/pack` 直接生效，不会被压住。
实测 `atm10_localization` 那 2785 个键里，模组 jar 提供 en_us 的 2666 条、jar 自带 zh_cn 的
2267 条、本包资源包已覆盖 2682 条；三处都没有的只有 103 条，集中在 ATM 自己用 kubejs
注册的炼金硫（`item.kubejs.*_sulfur`），而 `theurgy` 不在本包——这 103 条要先证可达再算缺口。

### 五、剩下的翻译

- 任务书约 1950 条 / 26.6 万英文字符，其中 `quest_desc` 800 条占 24.5 万字符；
  `title` 838 条里 96 条整串是已有中文的物品名，可机器填；另有 312 条 `quest_subtitle`。
- 资源包缺口 1712 条（占 SKY 全部 en_us 的 1.8%），集中在
  `chisel_chipped_integration` 501、`mcwpaintings` 180、`retrofactorymanager` 148、
  `logisticsnetworks` 103、`ars_nouveau` 98。

## 判定官方字节的办法

装好的实例目录**不能整个当官方文件用**：一部分文件在首次启动时被模组改写过。
按 mtime 分即可——安装窗口之内的是安装态，晚于启动时刻的是被改写过的。

本仓库对应的那个实例实测：1858 个文件里 1532 个与 `modpack.cfg` 同一秒（解出来的
overrides），321 个 `mods/*.jar` 加 4 个清单文件晚 53–56 秒（下载阶段），最晚的一个
也在安装开始后 56 秒之内；没有 `options.txt`、`logs/`、`saves/`、`crash-reports/`。
也就是**一次都没启动过**，整棵树可以当官方字节用。

更硬的一条旁证：CI 从 CurseForge 现取的 321 个 jar，sha256 与这棵树逐个相同，
一个不差。判「是不是官方字节」优先拿这个比，mtime 只是快速筛子。

两件由此而来的事实：
- `default_resource_packs.txt` **现在填不了**——那一行要游戏自己写出 `options.txt`
  才有，而这个实例从没启动过。要填必须先干净地启动一次、正常退出。
- 换实例之前先重新按 mtime 核一遍，再拿 `jars.json` 对 sha256。
