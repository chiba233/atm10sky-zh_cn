# 留空：这一版的顺序还没实测到。2.0.6 实例的 options.txt 里 resourcePacks 仍是空表，
# 说明它还没有一次写出内置包顺序的完整启动。
#
# 这一串必须真起一次该版实例、干净退出才拿得到，不许抄别的版本——内置包集合会变，
# 抄来的顺序会让汉化包被压在内置包底下，而且没有任何提示。
# 留空时安装器不伪造 resourcePacks 这一行，只提示玩家先启动一次游戏。
