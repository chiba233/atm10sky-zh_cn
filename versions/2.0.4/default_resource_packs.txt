# ATM10 Sky 2.0.4 首次启动后游戏自己写出来的 resourcePacks 顺序（不含本汉化包）。
# 安装器新建 options.txt 时照抄这一串、把汉化包放最后一位；不这么做的话游戏会把
# 这些内置包全插到汉化包后面，汉化包会被压在底下，等于没启用。
#
# **尚未填写。** 这一串必须真起一次实例、让游戏把 options.txt 写全之后逐项照抄：
# 游戏只跑到一半时读到的是 resourcePacks:[]，那是还没写出来的中间态，不能采信；
# 抄别的整合包的顺序同样不行，内置包集合不一样。
# 留空时安装器不伪造这一行，只提示玩家先启动一次——失效安全。
